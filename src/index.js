import React from 'react';
import ReactDOM from 'react-dom';
import UserReviewsForm from './UserReviewsForm'

ReactDOM.render(
   <React.StrictMode >
      <UserReviewsForm />
 {/* место второй компонент со списком отзывов  */}
   </React.StrictMode>

, document.getElementById('root'))